Steps to get the files:
Download from Google Docs as an `.html` file
Unzip the file
Rename the file to be lower cased
Run `deno fmt` to format the HTML so I can actually read it.
Add `margin: 0 auto;` to the body.
Upload it here.
